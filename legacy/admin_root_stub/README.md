# Admin Directory Moved

The contents of this directory have been moved.

- **Admin Pages (HTML)**: Now located at `site/admin/`.
- **Legacy Files**: Moved to `legacy/admin_root/`.
- **Other Files**: Moved to `legacy/admin_root_remaining/`.

This directory is kept temporarily to avoid confusion but should be considered deprecated.
For development, please use `site/admin/` and `src/`.
